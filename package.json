{
  "name": "js-mastery-atlas",
  "private": true,
  "packageManager": "bun@1.3.9",
  "scripts": {
    "sync:upstream": "bun scripts/sync-upstream.mjs",
    "parse:questions": "bun scripts/parse-readme.mjs",
    "content:refresh": "bun run sync:upstream && bun run parse:questions",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint:repo": "eslint . --config eslint.config.mjs",
    "lint": "bun run lint:repo && turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "prepare": "husky"
  },
  "workspaces": [
    "apps/web"
  ],
  "lint-staged": {
    "apps/web/**/*.{js,ts,tsx,mjs,cjs}": [
      "bunx eslint --config apps/web/eslint.config.mjs --fix"
    ],
    "*.{js,mjs,cjs}": [
      "bunx eslint --config eslint.config.mjs --fix"
    ],
    "scripts/**/*.{js,mjs,cjs}": [
      "bunx eslint --config eslint.config.mjs --fix"
    ],
    "*.md": [
      "bunx markdownlint-cli2"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "eslint": "9.39.4",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "markdownlint-cli2": "^0.22.0",
    "turbo": "^2.8.20"
  }
}
